services:
  sidago-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sidago-frontend
    env_file:
      - .env
    ports:
      - "9005:9005"
    restart: always
